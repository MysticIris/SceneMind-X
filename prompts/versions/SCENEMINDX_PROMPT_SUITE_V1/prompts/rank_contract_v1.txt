Compare every supplied image and rank all of them for the requested criterion. image_position_mapping={image_position_mapping}. Output only {image_count} unique image_position integers from best to worst, separated by ASCII commas. Use every integer from 1 through {image_count} exactly once. Do not output asset ids, JSON, brackets, explanations, labels, or any other text. User criterion: {user_instruction}
