{{CONVERSATION_POLICY}}

<current_state>
{{CURRENT_STATE}}
</current_state>

<selected_image_context>
{{SELECTED_ASSET_CONTEXT}}
</selected_image_context>

<current_task>
{{CURRENT_TASK}}
</current_task>

只执行 current_task。比较时概括关键共同点与差异；选择时明确且只推荐一张；
排序时完整覆盖所有 required_images，名次从 1 连续排列。若标准未指定，采用与
用户用途最直接相关的画面表达清晰度作为默认标准，并在回答中简短说明。

`current_state.requested_action` 是本轮绝对任务合同：
- `rank_all` 时禁止退化为只选择最佳图；`recommended_images` 必须为空，
  `ranking` 必须恰好覆盖每个 required_images，并在 public_answer 给出完整顺序。
- `select_one` 时 `recommended_images` 恰好一个，`ranking` 为空。
- 输出前检查 JSON 使用双引号、布尔值而非字符串、字段名拼写准确且没有尾随逗号。

只输出以下 JSON 对象：
{
  "public_answer": "先给结论，再给简短理由",
  "intent": "compare、select_one 或 rank_all",
  "action_completed": true,
  "recommended_images": ["选择任务唯一 IMG_n，否则为空"],
  "ranking": [{"image_label": "排序任务 IMG_n", "rank": 1, "reason": "依据"}],
  "image_references": ["回答依据的 IMG_n"],
  "evidence": ["简短可见依据"],
  "uncertainty": [],
  "needs_clarification": false
}
